{% extends "_layout.twig" %}

{% block content %}
	<div class="text-center">
		<h2>Welcome!</h2>
		<a href="{{ path_for('registration', {'role': 'patrol-leader'}) }}" class="btn btn-primary">Registruj se jako
			Patrol
			Leader!</a><br/><br/>
		<a href="{{ path_for('registration', {'role': 'ist'}) }}" class="btn btn-primary">Registruj se jako
			International
			Service Team!</a><br/><br/>

		<a href="{{ path_for('loginAskEmail') }}" class="btn btn-link">Přihlásit se</a>
	</div>
{% endblock %}