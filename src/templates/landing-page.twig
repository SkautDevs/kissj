{% extends "_layout.twig" %}

{% block content %}
	<div class="text-center">
		<h2>Welcome!</h2>
		<a href="{{ path_for('registration', {'role': 'patrol-leader'}) }}" class="btn btn-primary">Registruj se jako
			Patrol
			Leader!</a><br/><br/>
		<a href="{{ path_for('registration', {'role': 'ist'}) }}" class="btn btn-primary">Registruj se jako
			International
			Service Team!</a><br/><br/>
		<a href="{{ path_for('registrationLostSoul') }}" class="btn">Nemám Patrol Leadera a stejně chci jet jako účastník!</a><br/><br/>

		<div class="text-center">
			<form action="{{ path_for('loginScreenAfterSent') }}" method="post">
				<div class="form-group">
					<label for="form-email">Přihlásit se</label>
					<input class="form-control" type="email" name="email" id="form-email" required/><br/>
				</div>
				<input type="submit" value="Poslat mail pro přihlášení" class="btn btn-primary"/>
			</form>
		</div>
	</div>
{% endblock %}