{% extends "emails/_mail-layout.twig" %}

{% block content %}
	<div class="text-center">
		<p>Ahoj!</p>
		<p>Přihlašuješ se do registrace jako {{ readableRole }} na akci {{ eventName }}<br/>
			<br/>
			Přihlásíš se klikem na tento link: <a href="{{ baseHostScheme }}{{ link }}">{{ baseHostScheme }}{{ link }}</a></p>
	</div>
{% endblock %}