{% extends "_layout.twig" %}

{% block content %}
	<div class="two-cards">
		<div class="card registration-theme">
			<h2>Vyber roli!</h2>
			<a href="{{ path_for('registration', {'role': 'patrol-leader', 'eventSlug': eventSlug}) }}" class="btn">Registruj
				se
				jako<br>
				Patrol Leader!</a><br/><br/>
			<a href="{{ path_for('registration', {'role': 'ist', 'eventSlug': eventSlug}) }}" class="btn">Registruj se
				jako<br>
				International Service Team!</a><br/><br/>
			<a href="{{ path_for('registrationLostSoul', {'eventSlug': eventSlug}) }}" class="btn">Nemám Patrol Leadera
				a stejně chci jet jako
				účastník!</a><br/><br/>
		</div>

		<div class="card open-theme">
			<h2>Nápověda dk rolím</h2>
			<p></p>
		</div>
	</div>
{% endblock %}
