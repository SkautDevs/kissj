<!DOCTYPE html>
<html lang="{{ locale|default('cs') }}">

<head>
    <meta charset="utf-8"/>
    <meta name="robots" content="noindex, nofollow">

    <title>{% block pageTitle %}{{ '_layout.title' | trans }}{% endblock %}{% if event is defined %} - {{ event.readableName }}{% endif %} - kissj</title>

    <style>{{ pdfCss|default('')|raw }}</style>
</head>

<body>
    {% block content %}{% endblock %}
</body>
</html>
