<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8"/>
    <title>{% trans %}_mail-layout.registration{% endtrans %}{{ event.getReadableName }}</title>
    <style>
        .btn {
            background: #0b2a34;
            color: white;
            border-radius: 6px;
            text-decoration: none;
            font-size: 25px;
            display: block;
            text-align: center;
            padding: 15px;
            border: none;
        }

        .btn-small {
            display: inline-block;

        }

        .noselect {
            -webkit-touch-callout: none; /* iOS Safari */
            -webkit-user-select: none; /* Safari */
            -khtml-user-select: none; /* Konqueror HTML */
            -moz-user-select: none; /* Firefox */
            -ms-user-select: none; /* Internet Explorer/Edge */
            user-select: none; /* Non-prefixed version, currently supported by Chrome and Opera */
        }
    </style>
</head>
<body>
<main role="main" class="container">
    {% block content %}{% endblock %}

    <p>{% trans %}_mail-layout.ifQuestion{% endtrans %}
        <a href="{{ event.webUrl }}">{{ event.webUrl }}</a>
        {% trans %}_mail-layout.sendEmail{% endtrans %}<a href="mailto:{{ event.getContactEmail }}">{{ event.getContactEmail }}</a><span
                class="noselect">.</span></p>

    <p>{% trans %}_mail-layout.farewell{% endtrans %}{{ event.getReadableName }}</p>
</main>
</body>
</html>
