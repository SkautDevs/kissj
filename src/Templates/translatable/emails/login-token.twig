{% extends "emails/_mail-layout.twig" %}

{% block content %}
    <p>{% trans %}email.login-token.logging{% endtrans %}{{ event.readableName }}.</p>
    <a class="btn btn-small" href="{{ baseHostScheme }}{{ link }}">
        {% trans %}email.login-token.logIn{% endtrans %}
    </a>
    <p>{% trans %}email.login-token.buttonNotWorking{% endtrans %}
        <a href="{{ baseHostScheme }}{{ link }}">
            {{ baseHostScheme }}{{ link }}
        </a>
    </p>
{% endblock %}
