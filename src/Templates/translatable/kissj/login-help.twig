{% extends "_layout.twig" %}

{% block content %}
    <div class="card text-center">
        {% trans %}login-help.text{% endtrans %}
        <br>
        <a href="{{ url_for('landing', {'eventSlug': event.slug}) }}"
           class="btn btn-small btn-grey btn text-center">{% trans %}login-help.backToLogin{% endtrans %}</a>
    </div>
    Server time: {{ 'now'|date('Y-m-d H:i:m') }}, CET: {{ cej }}
{% endblock %}
